int main(void) 
{
  int a, b, res;
  
    printf("enter first integer:\n");
    scanf("%d", &a); // считываем целое значение в переменную a
    
    printf("enter second integer:\n");
    scanf("%d", &b); // считываем целое значение в переменную в

  res = a + b;  
  printf("Results: %d + %d = %d\n", a, b, res);

  return 0;
}